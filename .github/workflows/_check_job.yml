name: _Check Job Date

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch job posting date
        run: |
          python3 - <<'PYEOF'
          import urllib.request, json, re, time
          from datetime import datetime, timezone

          JOB_ID = "92372690618196678"
          UA = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0 Safari/537.36"
          DS1 = re.compile(r"AF_initDataCallback\(\{key:\s*'ds:1'.*?data:(\[.*?\])\s*,\s*sideChannel", re.S)

          # Try the direct job URL first
          url = f"https://www.google.com/about/careers/applications/jobs/results/{JOB_ID}-data-engineer"
          req = urllib.request.Request(url, headers={"User-Agent": UA})
          with urllib.request.urlopen(req, timeout=30) as r:
              html = r.read().decode("utf-8", errors="replace")

          m = DS1.search(html)
          if not m:
              print("ds:1 block not found")
              exit(1)

          ds1 = json.loads(m.group(1))
          raw_jobs = ds1[0] if ds1 and isinstance(ds1[0], list) else []

          for job in raw_jobs:
              if not isinstance(job, list) or not job:
                  continue
              if str(job[0]) == JOB_ID:
                  print(f"Found job: {job[1]}")
                  ts = job[12][0] if len(job) > 12 and isinstance(job[12], list) and job[12] else None
                  if ts:
                      dt = datetime.fromtimestamp(ts, tz=timezone.utc)
                      delta = int(time.time()) - ts
                      days  = delta // 86400
                      print(f"Posted: {dt.strftime('%Y-%m-%d %H:%M:%S UTC')}  ({days} days ago)")
                  else:
                      print("No timestamp found")
                  break
          else:
              print(f"Job {JOB_ID} not found in page results — may not appear in search listing")
          PYEOF
