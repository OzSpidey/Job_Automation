# Credentials / secrets
.env
applicant_info_secret.txt

# Python
__pycache__/
*.pyc
*.pyo

# Node
node_modules/
package-lock.json

# Debug artifacts
*.png
*.html
*.log

# Session / output files
*_session.json
emails_output.*
linkedin_urls.txt
emails_outreach_sent.json
naukri_output.txt
*.bak

# Local runners (bat files are local only)
*.bat

# LinkedIn job applier (runs locally only)
linkedin_job_applier.py
linkedin_applied_ids.json
linkedin_jobs_applied.csv
linkedin_cookie.txt
.github/workflows/linkedin_job_applier.yml

# Retired scripts — local-only archive of deprecated scrapers
retired_scripts/

# Greenhouse scraper — slug list is private; historical CSV is local-only
greenhouse_companies.json
csv/greenhouse_nologin_jobs.csv

# Lever scraper — slug list is private
lever_companies.json

# Ashby scraper — slug list is private
ashby_companies.json

# Claude internal
.claude/
